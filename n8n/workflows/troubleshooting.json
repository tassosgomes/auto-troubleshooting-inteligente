  "jsCode": "const { randomUUID } = require('crypto');\nconst Handlebars = require('handlebars');\n\nHandlebars.registerHelper('inc', (index) => Number(index) + 1);\n\nconst template = [\n  '# 🔍 Diagnóstico de Incidente',\n  '',\n  '**Ticket ID:** `{{ticket_id}}`  ',\n  '**Data:** {{timestamp}}  ',\n  '**Serviço:** {{service_name}}  ',\n  '**Namespace:** {{namespace}}  ',\n  '',\n  '---',\n  '',\n  '## 📋 Resumo Executivo',\n  '',\n  '{{summary}}',\n  '',\n  '## 🏷️ Classificação',\n  '',\n  '| Campo | Valor |',\n  '|-------|-------|',\n  '| **Tipo** | {{classification}} |',\n  '| **Severidade** | {{severity}} |',\n  '| **Confiança** | {{confidence}} |',\n  '',\n  '---',\n  '',\n  '## 🔎 Evidências Coletadas',\n  '',\n  '### Erro Original',\n  '',\n  '```',\n  '{{error_message}}',\n  '```',\n  '',\n  '{{#if stack_trace}}',\n  '### Stack Trace',\n  '',\n  '```',\n  '{{stack_trace}}',\n  '```',\n  '{{/if}}',\n  '',\n  '{{#if kubernetes_evidence}}',\n  '### Análise de Kubernetes',\n  '',\n  '{{kubernetes_evidence}}',\n  '{{/if}}',\n  '',\n  '{{#if code_evidence}}',\n  '### Análise de Código',\n  '',\n  '{{code_evidence}}',\n  '{{/if}}',\n  '',\n  '{{#if network_evidence}}',\n  '### Teste de Conectividade',\n  '',\n  '{{network_evidence}}',\n  '{{/if}}',\n  '',\n  '---',\n  '',\n  '## 🎯 Causa Raiz Identificada',\n  '',\n  '{{root_cause}}',\n  '',\n  '---',\n  '',\n  '## 💡 Sugestões de Correção',\n  '',\n  '{{#each suggestions}}',\n  '{{inc @index}}. {{this}}',\n  '{{/each}}',\n  '',\n  '---',\n  '',\n  '## ⏭️ Próximos Passos',\n  '',\n  '{{#each next_steps}}',\n  '- [ ] {{this}}',\n  '{{/each}}',\n  '',\n  '---',\n  '',\n  '*Este diagnóstico foi gerado automaticamente pelo Auto-Troubleshooting Inteligente.*',\n  '',\n  '*Para análise adicional, você pode usar GitHub Copilot ou Claude Code com este relatório.*',\n  '',\n  '📧 [Dar feedback sobre este diagnóstico]({{feedback_url}})',\n].join('\\n');\n\nconst SECRET_PATTERN = /(password|pass|pwd|secret|token|api[_-]?key|authorization|private[_-]?key|client_secret|access[_-]?token|refresh[_-]?token)\\s*[:=]\\s*[^\\s,;]+/gi;\n\nfunction sanitizeText(text) {\n  if (!text) return '';\n  return String(text).replace(SECRET_PATTERN, (match) => {\n    const key = match.split(/[:=]/)[0];\n    return `${key}: [REDACTED]`;\n  });\n}\n\nfunction sanitizeArray(value) {\n  if (!Array.isArray(value)) return [];\n  return value.map((entry) => sanitizeText(entry)).filter(Boolean);\n}\n\nfunction formatKubernetesEvidence(podDescription, events) {\n  if (!podDescription && !events) return '';\n\n  let output = '';\n  if (podDescription?.name) {\n    output += `**Pod:** ${sanitizeText(podDescription.name)}\\n`;\n  }\n  if (podDescription?.status) {\n    output += `**Status:** ${sanitizeText(podDescription.status)}\\n\\n`;\n  }\n\n  if (podDescription?.containerStatuses?.length) {\n    output += '**Containers:**\\n';\n    for (const container of podDescription.containerStatuses) {\n      const readyLabel = container.ready ? '✅ Ready' : '❌ Not Ready';\n      output += `- ${sanitizeText(container.name)}: ${readyLabel}`;\n      if (Number.isFinite(container.restartCount)) {\n        output += ` (Restarts: ${container.restartCount})`;\n      }\n      output += '\\n';\n\n      if (container.state?.terminated) {\n        output += `  - Terminated: ${sanitizeText(container.state.terminated.reason)}\\n`;\n      }\n      if (container.state?.waiting) {\n        output += `  - Waiting: ${sanitizeText(container.state.waiting.reason)}\\n`;\n      }\n    }\n  }\n\n  if (events?.length) {\n    output += '\\n**Eventos Recentes:**\\n';\n    for (const event of events.slice(-5)) {\n      const icon = event.type === 'Warning' ? '⚠️' : 'ℹ️';\n      output += `${icon} [${sanitizeText(event.reason)}] ${sanitizeText(event.message)}\\n`;\n    }\n  }\n\n  return output.trim();\n}\n\nfunction formatCodeEvidence(filePath, lineNumber, codeSnippet, language = '') {\n  if (!codeSnippet) return '';\n\n  const lines = String(codeSnippet).split('\\n');\n  const safeLineNumber = Number(lineNumber) || 1;\n  const startLine = Math.max(1, safeLineNumber - 5);\n\n  let output = `**Arquivo:** \\`${sanitizeText(filePath || 'arquivo desconhecido')}\\`\\n`;\n  output += `**Linha:** ${safeLineNumber}\\n\\n`;\n  output += `\\`\\`\\`${sanitizeText(language)}\\n`;\n\n  lines.forEach((line, index) => {\n    const currentLine = startLine + index;\n    const marker = currentLine === safeLineNumber ? '>>>' : '   ';\n    output += `${marker} ${currentLine}: ${sanitizeText(line)}\\n`;\n  });\n\n  output += '\\`\\`\\`\\n';\n\n  return output.trim();\n}\n\nfunction formatNetworkEvidence(networkInfo) {\n  if (!networkInfo) return '';\n  if (typeof networkInfo === 'string') return sanitizeText(networkInfo);\n\n  const url = networkInfo.url ? `**URL:** ${sanitizeText(networkInfo.url)}\\n` : '';\n  const status = networkInfo.status_code ? `**Status:** ${sanitizeText(networkInfo.status_code)}\\n` : '';\n  const responseTime = networkInfo.response_time_ms\n    ? `**Tempo de resposta:** ${sanitizeText(networkInfo.response_time_ms)}ms\\n`\n    : '';\n  const error = networkInfo.error ? `**Erro:** ${sanitizeText(networkInfo.error)}\\n` : '';\n\n  return `${url}${status}${responseTime}${error}`.trim();\n}\n\nfunction buildPartialAnalysis(kubernetesEvidence, codeEvidence, networkEvidence, triageReasoning) {\n  const sections = [];\n  if (triageReasoning) {\n    sections.push(`### Triagem\\n\\n${sanitizeText(triageReasoning)}`);\n  }\n  if (kubernetesEvidence) {\n    sections.push(`### Análise de Kubernetes\\n\\n${sanitizeText(kubernetesEvidence)}`);\n  }\n  if (codeEvidence) {\n    sections.push(`### Análise de Código\\n\\n${sanitizeText(codeEvidence)}`);\n  }\n  if (networkEvidence) {\n    sections.push(`### Teste de Conectividade\\n\\n${sanitizeText(networkEvidence)}`);\n  }\n  return sections.join('\\n\\n').trim();\n}\n\nfunction normalize(value, allowed, fallback) {\n  const normalized = String(value || fallback).toLowerCase();\n  return allowed.includes(normalized) ? normalized : fallback;\n}\n\nconst input = $json;\nconst ticketId = input.ticket_id || input.ticketId || randomUUID();\nconst timestamp = input.timestamp || new Date().toISOString();\n\nconst podDescription = input.pod_description\n  || input.kubernetes_pod_description\n  || input.kubernetes?.podDescription\n  || input.kubernetes?.pod\n  || null;\n\nconst events = input.events\n  || input.kubernetes_events\n  || input.kubernetes?.events\n  || null;\n\nconst kubernetesEvidence = input.kubernetes_evidence\n  || formatKubernetesEvidence(podDescription, events);\n\nconst codeEvidenceDetail = input.code_evidence_detail\n  || input.code\n  || null;\n\nconst codeEvidence = typeof input.code_evidence === 'string'\n  ? input.code_evidence\n  : (codeEvidenceDetail\n    ? formatCodeEvidence(\n      codeEvidenceDetail.filePath || codeEvidenceDetail.file_path,\n      codeEvidenceDetail.lineNumber || codeEvidenceDetail.line_number,\n      codeEvidenceDetail.codeSnippet || codeEvidenceDetail.code_snippet,\n      codeEvidenceDetail.language || codeEvidenceDetail.lang || ''\n    )\n    : '');\n\nconst networkEvidence = input.network_evidence\n  || formatNetworkEvidence(input.network || null);\n\nconst suggestions = sanitizeArray(input.suggestions);\nconst nextSteps = sanitizeArray(input.next_steps || input.nextSteps);\n\nconst triageReasoning = input.triage_reasoning || '';\nlet analysisPartial = Boolean(input.analysis_partial);\nlet fallbackReason = input.fallback_reason || null;\n\nif (input.llm_fallback) {\n  analysisPartial = true;\n  fallbackReason = fallbackReason || 'Serviço de IA indisponível';\n}\n\nconst failureSources = [];\nconst failureMap = [\n  { key: 'kubernetes_error', label: 'Kubernetes' },\n  { key: 'k8s_error', label: 'Kubernetes' },\n  { key: 'git_error', label: 'Git' },\n  { key: 'clone_error', label: 'Git' },\n];\n\nfor (const failure of failureMap) {\n  if (input[failure.key]) {\n    failureSources.push(failure.label);\n  }\n}\n\nif (failureSources.length) {\n  analysisPartial = true;\n  fallbackReason = fallbackReason || `Falha ao acessar: ${Array.from(new Set(failureSources)).join(', ')}`;\n  console.error(JSON.stringify({\n    level: 'ERROR',\n    timestamp: new Date().toISOString(),\n    step: 'PARTIAL_ANALYSIS',\n    service_name: input.service_name,\n    namespace: input.namespace,\n    error_type: 'DependencyFailure',\n    error_message: fallbackReason\n  }));\n}\n\nconst partialAnalysis = buildPartialAnalysis(kubernetesEvidence, codeEvidence, networkEvidence, triageReasoning);\n\nconst reportData = {\n  ticket_id: ticketId,\n  timestamp,\n  service_name: sanitizeText(input.service_name || 'serviço não informado'),\n  namespace: sanitizeText(input.namespace || 'namespace não informado'),\n  summary: sanitizeText(input.summary) || 'Resumo não fornecido; revisar evidências coletadas.',\n  classification: normalize(input.classification, ['infrastructure', 'code', 'unknown'], 'unknown'),\n  severity: normalize(input.severity, ['critical', 'high', 'medium', 'low'], 'medium'),\n  confidence: normalize(input.confidence, ['alta', 'média', 'baixa'], 'média'),\n  error_message: sanitizeText(input.error_message || 'Erro não informado'),\n  stack_trace: sanitizeText(input.stack_trace || ''),\n  kubernetes_evidence: sanitizeText(kubernetesEvidence),\n  code_evidence: sanitizeText(codeEvidence),\n  network_evidence: sanitizeText(networkEvidence),\n  root_cause: sanitizeText(input.root_cause) || 'Causa raiz não identificada automaticamente.',\n  suggestions: suggestions.length\n    ? suggestions\n    : ['Revisar logs completos e métricas do serviço', 'Validar configurações e limites de recursos'],\n  next_steps: nextSteps.length\n    ? nextSteps\n    : ['Confirmar correção em ambiente de homologação', 'Monitorar o serviço após o ajuste'],\n  feedback_url: sanitizeText(input.feedback_url || input.feedbackUrl || input.feedback)\n    || `https://feedback.local/diagnosis/${ticketId}`,\n  analysis_partial: analysisPartial,\n  fallback_reason: fallbackReason,\n  partial_analysis: partialAnalysis,\n};\n\nconst compiled = Handlebars.compile(template, { noEscape: true });\nconst report = compiled(reportData);\n\nreturn [\n  {\n    json: {\n      ...input,\n      ticket_id: ticketId,\n      timestamp,\n      diagnosis_report: report,\n      report_markdown: report,\n      report_data: reportData,\n      analysis_partial: analysisPartial,\n      fallback_reason: fallbackReason,\n      partial_analysis: partialAnalysis,\n    },\n  },\n];"
{
  "name": "Recepcao de Alertas - Auto Troubleshooting",
  "nodes": [
    {
      "parameters": {
        "path": "alert",
        "httpMethod": "POST",
        "responseMode": "responseNode"
      },
      "id": "1",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [200, 300],
      "webhookId": "b2e6f1d2-4f8c-4a1b-9b0f-6c7d7b1c2a10"
    },
    {
      "parameters": {
        "language": "javascript",
        "jsCode": "const required = ['service_name', 'namespace', 'pod_name', 'error_message'];\nconst item = $input.first().json;\nconst payload = item?.body ?? item;\nconst missing = required.filter((field) => !payload?.[field]);\n\nreturn [\n  {\n    json: {\n      ...payload,\n      isValid: missing.length === 0,\n      missing,\n      errorMessage: missing.length\n        ? `Campos obrigatórios faltando: ${missing.join(', ')}`\n        : null,\n    },\n  },\n];"
      },
      "id": "2",
      "name": "Validar Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [420, 300]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{$json.isValid}}",
              "value2": true
            }
          ]
        }
      },
      "id": "3",
      "name": "Payload valido?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [640, 300]
    },
    {
      "parameters": {
        "language": "javascript",
        "jsCode": "const alert = $json;\nconsole.log('[ALERT] Webhook recebido', JSON.stringify(alert));\nreturn $input.all();"
      },
      "id": "4",
      "name": "Log Alert",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [860, 240]
    },
    {
      "parameters": {
        "language": "javascript",
        "jsCode": "const alert = $json;\n\nconst systemPrompt = `# INSTRUÇÕES DO ASSISTENTE DE DIAGNÓSTICO\n\nVocê é um especialista em diagnóstico de incidentes de software. Sua tarefa é analisar alertas de erro e fornecer diagnósticos precisos e acionáveis.\n\n## FLUXO DE ANÁLISE\n\n1. **TRIAGEM**: Classifique o erro em uma das categorias:\n   - \`infrastructure\`: Problemas de Kubernetes (OOMKilled, CrashLoopBackOff, probes, recursos)\n   - \`code\`: Bugs de aplicação (exceções, null pointers, erros de lógica)\n   - \`unknown\`: Casos inconclusivos que precisam investigação adicional\n\n2. **INVESTIGAÇÃO**: Use as ferramentas disponíveis para coletar evidências:\n   - Para \`infrastructure\`: use tools de Kubernetes (describePod, getEvents, etc.)\n   - Para \`code\`: use tools de Git (cloneRepo, readFile) para analisar o código\n   - Para \`unknown\`: use httpRequest para testar conectividade se relevante\n\n3. **DIAGNÓSTICO**: Produza um relatório com:\n   - Resumo executivo (2-3 frases)\n   - Classificação e confiança\n   - Evidências coletadas\n   - Causa raiz identificada\n   - Sugestões de correção\n\n## INSTRUÇÕES DE SEGURANÇA (OBRIGATÓRIAS)\n\nNUNCA inclua no relatório:\n- Valores de variáveis de ambiente\n- Valores de Secrets do Kubernetes\n- Senhas, tokens, API keys ou credenciais\n- Dados pessoais (PII)\n\nAo mencionar Secrets ou ConfigMaps, liste APENAS os nomes das chaves, NUNCA os valores.\n\n✅ CORRETO: \"O Secret \\\"db-credentials\\\" possui as chaves: username, password, host\"\n❌ INCORRETO: \"O Secret \\\"db-credentials\\\" contém: username=admin, password=abc123\"\n\n## FORMATO DE RESPOSTA\n\nResponda sempre em JSON estruturado:\n\n{\n  \"classification\": \"infrastructure|code|unknown\",\n  \"confidence\": \"alta|média|baixa\",\n  \"reasoning\": \"Breve explicação da classificação\"\n}\n`;\n\nconst triagePrompt = `Analise o seguinte alerta de erro e classifique-o:\n\n**Serviço:** ${alert.service_name ?? ''}\n**Namespace:** ${alert.namespace ?? ''}\n**Pod:** ${alert.pod_name ?? ''}\n**Erro:** ${alert.error_message ?? ''}\n**Stack Trace:** ${alert.stack_trace ?? ''}\n\nResponda APENAS com JSON:\n{\n  \"classification\": \"infrastructure|code|unknown\",\n  \"confidence\": \"alta|média|baixa\",\n  \"reasoning\": \"Breve explicação da classificação\"\n}`;\n\nconst provider = ($env.LLM_PROVIDER || 'openai').toLowerCase();\nconst model = $env.LLM_MODEL || 'gpt-5.2';\n\nconst timeoutSeconds = Number($env.LLM_TIMEOUT_SECONDS ?? 60);\nconst timeoutMs = Number.isFinite(timeoutSeconds) && timeoutSeconds > 0\n  ? timeoutSeconds * 1000\n  : 60000;\n\nconst openAiPayload = {\n  model,\n  messages: [\n    { role: 'system', content: systemPrompt },\n    { role: 'user', content: triagePrompt },\n  ],\n  temperature: 0.2,\n};\n\nconst geminiPayload = {\n  contents: [\n    { role: 'user', parts: [{ text: triagePrompt }] },\n  ],\n  systemInstruction: { parts: [{ text: systemPrompt }] },\n  generationConfig: { temperature: 0.2, responseMimeType: 'application/json' },\n};\n\nconst requestOptions = provider === 'gemini' ? {\n  method: 'POST',\n  url: 'https://generativelanguage.googleapis.com/v1/models/gemini-3.0-pro:generateContent',\n  headers: {\n    'content-type': 'application/json',\n    'x-goog-api-key': $env.LLM_API_KEY || '',\n  },\n  body: geminiPayload,\n  json: true,\n  timeout: timeoutMs,\n} : {\n  method: 'POST',\n  url: 'https://api.openai.com/v1/chat/completions',\n  headers: {\n    'content-type': 'application/json',\n    Authorization: `Bearer ${$env.LLM_API_KEY || ''}`\n  },\n  body: openAiPayload,\n  json: true,\n  timeout: timeoutMs,\n};\n\nfunction extractJson(text) {\n  if (!text) return null;\n  const cleaned = text.replace(/```json|```/g, '').trim();\n  try {\n    return JSON.parse(cleaned);\n  } catch {\n    return null;\n  }\n}\n\nfunction heuristicClassification(message, stack) {\n  const text = `${message ?? ''} ${stack ?? ''}`.toLowerCase();\n  if (/(oomkilled|crashloopbackoff|liveness|readiness|probe|resource limit|memory|cpu|evicted|nodepressure)/.test(text)) {\n    return { classification: 'infrastructure', confidence: 'média', reasoning: 'Padrões típicos de infraestrutura detectados.' };\n  }\n  if (/(nullpointerexception|npe|indexoutofboundsexception|typeerror|attributeerror|syntax error|compilation)/.test(text)) {\n    return { classification: 'code', confidence: 'média', reasoning: 'Padrões típicos de erro de aplicação detectados.' };\n  }\n  if (/(timeout|timed out|connection refused|refused|connection reset|5\\d{2})/.test(text)) {\n    return { classification: 'unknown', confidence: 'média', reasoning: 'Indícios de falha externa ou timeout.' };\n  }\n  return { classification: 'unknown', confidence: 'baixa', reasoning: 'Sem padrões claros; requer investigação.' };\n}\n\nlet result;\nlet analysisPartial = false;\nlet fallbackReason = null;\nlet llmFallback = false;\ntry {\n  const response = await this.helpers.httpRequest(requestOptions);\n  const rawText = provider === 'gemini'\n    ? response?.candidates?.[0]?.content?.parts?.map((p) => p.text).join('')\n    : response?.choices?.[0]?.message?.content;\n\n  const parsed = extractJson(rawText);\n  if (parsed?.classification) {\n    result = {\n      classification: String(parsed.classification).toLowerCase(),\n      confidence: parsed.confidence ?? 'média',\n      reasoning: parsed.reasoning ?? ''\n    };\n  } else {\n    llmFallback = true;\n    analysisPartial = true;\n    fallbackReason = 'Resposta inválida do LLM';\n    console.error(JSON.stringify({\n      level: 'ERROR',\n      timestamp: new Date().toISOString(),\n      step: 'LLM_TRIAGE',\n      service_name: alert.service_name,\n      namespace: alert.namespace,\n      error_type: 'InvalidLLMResponse',\n      error_message: 'Resposta do LLM não pôde ser interpretada.'\n    }));\n    result = heuristicClassification(alert.error_message, alert.stack_trace);\n  }\n} catch (error) {\n  llmFallback = true;\n  analysisPartial = true;\n  fallbackReason = error?.message || 'Erro ao chamar LLM';\n  console.error(JSON.stringify({\n    level: 'ERROR',\n    timestamp: new Date().toISOString(),\n    step: 'LLM_TRIAGE',\n    service_name: alert.service_name,\n    namespace: alert.namespace,\n    error_type: error?.name || 'UnknownError',\n    error_message: error?.message || 'Erro desconhecido',\n    stack: error?.stack\n  }));\n  result = heuristicClassification(alert.error_message, alert.stack_trace);\n}\n\nconst normalized = ['infrastructure', 'code', 'unknown'].includes(result.classification)\n  ? result.classification\n  : 'unknown';\n\nconst confidence = ['alta', 'média', 'baixa'].includes(result.confidence)\n  ? result.confidence\n  : 'média';\n\nreturn [\n  {\n    json: {\n      ...alert,\n      classification: normalized,\n      confidence,\n      triage_reasoning: result.reasoning ?? '',\n      llm_provider: provider,\n      llm_model: model,\n      llm_fallback: llmFallback,\n      analysis_partial: analysisPartial,\n      fallback_reason: fallbackReason,\n    }\n  }\n];"
      },
      "id": "8",
      "name": "LLM - Triagem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1080, 120]
    },
    {
      "parameters": {
        "dataType": "string",
        "value1": "={{$json.classification}}",
        "rules": [
          {
            "operation": "equals",
            "value2": "infrastructure"
          },
          {
            "operation": "equals",
            "value2": "code"
          },
          {
            "operation": "equals",
            "value2": "unknown"
          }
        ]
      },
      "id": "9",
      "name": "Classificacao?",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 2,
      "position": [1300, 120]
    },
    {
      "parameters": {
        "keepOnlySet": false,
        "values": {
          "string": [
            {
              "name": "route",
              "value": "infrastructure"
            }
          ]
        }
      },
      "id": "10",
      "name": "Rota Infrastructure",
      "type": "n8n-nodes-base.set",
      "typeVersion": 2,
      "position": [1520, 40]
    },
    {
      "parameters": {
        "keepOnlySet": false,
        "values": {
          "string": [
            {
              "name": "route",
              "value": "code"
            }
          ]
        }
      },
      "id": "11",
      "name": "Rota Code",
      "type": "n8n-nodes-base.set",
      "typeVersion": 2,
      "position": [1520, 120]
    },
    {
      "parameters": {
        "keepOnlySet": false,
        "values": {
          "string": [
            {
              "name": "route",
              "value": "unknown"
            }
          ]
        }
      },
      "id": "12",
      "name": "Rota Unknown",
      "type": "n8n-nodes-base.set",
      "typeVersion": 2,
      "position": [1520, 200]
    },
    {
      "parameters": {
        "language": "javascript",
        "jsCode": "const { randomUUID } = require('crypto');\nconst Handlebars = require('handlebars');\n\nHandlebars.registerHelper('inc', (index) => Number(index) + 1);\n\nconst template = [\n  '# 🔍 Diagnóstico de Incidente',\n  '',\n  '**Ticket ID:** `{{ticket_id}}`  ',\n  '**Data:** {{timestamp}}  ',\n  '**Serviço:** {{service_name}}  ',\n  '**Namespace:** {{namespace}}  ',\n  '',\n  '---',\n  '',\n  '## 📋 Resumo Executivo',\n  '',\n  '{{summary}}',\n  '',\n  '## 🏷️ Classificação',\n  '',\n  '| Campo | Valor |',\n  '|-------|-------|',\n  '| **Tipo** | {{classification}} |',\n  '| **Severidade** | {{severity}} |',\n  '| **Confiança** | {{confidence}} |',\n  '',\n  '---',\n  '',\n  '## 🔎 Evidências Coletadas',\n  '',\n  '### Erro Original',\n  '',\n  '```',\n  '{{error_message}}',\n  '```',\n  '',\n  '{{#if stack_trace}}',\n  '### Stack Trace',\n  '',\n  '```',\n  '{{stack_trace}}',\n  '```',\n  '{{/if}}',\n  '',\n  '{{#if kubernetes_evidence}}',\n  '### Análise de Kubernetes',\n  '',\n  '{{kubernetes_evidence}}',\n  '{{/if}}',\n  '',\n  '{{#if code_evidence}}',\n  '### Análise de Código',\n  '',\n  '{{code_evidence}}',\n  '{{/if}}',\n  '',\n  '{{#if network_evidence}}',\n  '### Teste de Conectividade',\n  '',\n  '{{network_evidence}}',\n  '{{/if}}',\n  '',\n  '---',\n  '',\n  '## 🎯 Causa Raiz Identificada',\n  '',\n  '{{root_cause}}',\n  '',\n  '---',\n  '',\n  '## 💡 Sugestões de Correção',\n  '',\n  '{{#each suggestions}}',\n  '{{inc @index}}. {{this}}',\n  '{{/each}}',\n  '',\n  '---',\n  '',\n  '## ⏭️ Próximos Passos',\n  '',\n  '{{#each next_steps}}',\n  '- [ ] {{this}}',\n  '{{/each}}',\n  '',\n  '---',\n  '',\n  '*Este diagnóstico foi gerado automaticamente pelo Auto-Troubleshooting Inteligente.*',\n  '',\n  '*Para análise adicional, você pode usar GitHub Copilot ou Claude Code com este relatório.*',\n  '',\n  '📧 [Dar feedback sobre este diagnóstico]({{feedback_url}})',\n].join('\\n');\n\nconst SECRET_PATTERN = /(password|pass|pwd|secret|token|api[_-]?key|authorization|private[_-]?key|client_secret|access[_-]?token|refresh[_-]?token)\\s*[:=]\\s*[^\\s,;]+/gi;\n\nfunction sanitizeText(text) {\n  if (!text) return '';\n  return String(text).replace(SECRET_PATTERN, (match) => {\n    const key = match.split(/[:=]/)[0];\n    return `${key}: [REDACTED]`;\n  });\n}\n\nfunction sanitizeArray(value) {\n  if (!Array.isArray(value)) return [];\n  return value.map((entry) => sanitizeText(entry)).filter(Boolean);\n}\n\nfunction formatKubernetesEvidence(podDescription, events) {\n  if (!podDescription && !events) return '';\n\n  let output = '';\n  if (podDescription?.name) {\n    output += `**Pod:** ${sanitizeText(podDescription.name)}\\n`;\n  }\n  if (podDescription?.status) {\n    output += `**Status:** ${sanitizeText(podDescription.status)}\\n\\n`;\n  }\n\n  if (podDescription?.containerStatuses?.length) {\n    output += '**Containers:**\\n';\n    for (const container of podDescription.containerStatuses) {\n      const readyLabel = container.ready ? '✅ Ready' : '❌ Not Ready';\n      output += `- ${sanitizeText(container.name)}: ${readyLabel}`;\n      if (Number.isFinite(container.restartCount)) {\n        output += ` (Restarts: ${container.restartCount})`;\n      }\n      output += '\\n';\n\n      if (container.state?.terminated) {\n        output += `  - Terminated: ${sanitizeText(container.state.terminated.reason)}\\n`;\n      }\n      if (container.state?.waiting) {\n        output += `  - Waiting: ${sanitizeText(container.state.waiting.reason)}\\n`;\n      }\n    }\n  }\n\n  if (events?.length) {\n    output += '\\n**Eventos Recentes:**\\n';\n    for (const event of events.slice(-5)) {\n      const icon = event.type === 'Warning' ? '⚠️' : 'ℹ️';\n      output += `${icon} [${sanitizeText(event.reason)}] ${sanitizeText(event.message)}\\n`;\n    }\n  }\n\n  return output.trim();\n}\n\nfunction formatCodeEvidence(filePath, lineNumber, codeSnippet, language = '') {\n  if (!codeSnippet) return '';\n\n  const lines = String(codeSnippet).split('\\n');\n  const safeLineNumber = Number(lineNumber) || 1;\n  const startLine = Math.max(1, safeLineNumber - 5);\n\n  let output = `**Arquivo:** \\`${sanitizeText(filePath || 'arquivo desconhecido')}\\`\\n`;\n  output += `**Linha:** ${safeLineNumber}\\n\\n`;\n  output += `\\`\\`\\`${sanitizeText(language)}\\n`;\n\n  lines.forEach((line, index) => {\n    const currentLine = startLine + index;\n    const marker = currentLine === safeLineNumber ? '>>>' : '   ';\n    output += `${marker} ${currentLine}: ${sanitizeText(line)}\\n`;\n  });\n\n  output += '\\`\\`\\`\\n';\n\n  return output.trim();\n}\n\nfunction formatNetworkEvidence(networkInfo) {\n  if (!networkInfo) return '';\n  if (typeof networkInfo === 'string') return sanitizeText(networkInfo);\n\n  const url = networkInfo.url ? `**URL:** ${sanitizeText(networkInfo.url)}\\n` : '';\n  const status = networkInfo.status_code ? `**Status:** ${sanitizeText(networkInfo.status_code)}\\n` : '';\n  const responseTime = networkInfo.response_time_ms\n    ? `**Tempo de resposta:** ${sanitizeText(networkInfo.response_time_ms)}ms\\n`\n    : '';\n  const error = networkInfo.error ? `**Erro:** ${sanitizeText(networkInfo.error)}\\n` : '';\n\n  return `${url}${status}${responseTime}${error}`.trim();\n}\n\nfunction buildPartialAnalysis(kubernetesEvidence, codeEvidence, networkEvidence, triageReasoning) {\n  const sections = [];\n  if (triageReasoning) {\n    sections.push(`### Triagem\\n\\n${sanitizeText(triageReasoning)}`);\n  }\n  if (kubernetesEvidence) {\n    sections.push(`### Análise de Kubernetes\\n\\n${sanitizeText(kubernetesEvidence)}`);\n  }\n  if (codeEvidence) {\n    sections.push(`### Análise de Código\\n\\n${sanitizeText(codeEvidence)}`);\n  }\n  if (networkEvidence) {\n    sections.push(`### Teste de Conectividade\\n\\n${sanitizeText(networkEvidence)}`);\n  }\n  return sections.join('\\n\\n').trim();\n}\n\nfunction normalize(value, allowed, fallback) {\n  const normalized = String(value || fallback).toLowerCase();\n  return allowed.includes(normalized) ? normalized : fallback;\n}\n\nconst input = $json;\nconst ticketId = input.ticket_id || input.ticketId || randomUUID();\nconst timestamp = input.timestamp || new Date().toISOString();\n\nconst podDescription = input.pod_description\n  || input.kubernetes_pod_description\n  || input.kubernetes?.podDescription\n  || input.kubernetes?.pod\n  || null;\n\nconst events = input.events\n  || input.kubernetes_events\n  || input.kubernetes?.events\n  || null;\n\nconst kubernetesEvidence = input.kubernetes_evidence\n  || formatKubernetesEvidence(podDescription, events);\n\nconst codeEvidenceDetail = input.code_evidence_detail\n  || input.code\n  || null;\n\nconst codeEvidence = typeof input.code_evidence === 'string'\n  ? input.code_evidence\n  : (codeEvidenceDetail\n    ? formatCodeEvidence(\n      codeEvidenceDetail.filePath || codeEvidenceDetail.file_path,\n      codeEvidenceDetail.lineNumber || codeEvidenceDetail.line_number,\n      codeEvidenceDetail.codeSnippet || codeEvidenceDetail.code_snippet,\n      codeEvidenceDetail.language || codeEvidenceDetail.lang || ''\n    )\n    : '');\n\nconst networkEvidence = input.network_evidence\n  || formatNetworkEvidence(input.network || null);\n\nconst suggestions = sanitizeArray(input.suggestions);\nconst nextSteps = sanitizeArray(input.next_steps || input.nextSteps);\n\nconst triageReasoning = input.triage_reasoning || '';\nlet analysisPartial = Boolean(input.analysis_partial);\nlet fallbackReason = input.fallback_reason || null;\n\nif (input.llm_fallback) {\n  analysisPartial = true;\n  fallbackReason = fallbackReason || 'Serviço de IA indisponível';\n}\n\nconst failureSources = [];\nconst failureMap = [\n  { key: 'kubernetes_error', label: 'Kubernetes' },\n  { key: 'k8s_error', label: 'Kubernetes' },\n  { key: 'git_error', label: 'Git' },\n  { key: 'clone_error', label: 'Git' },\n];\n\nfor (const failure of failureMap) {\n  if (input[failure.key]) {\n    failureSources.push(failure.label);\n  }\n}\n\nif (failureSources.length) {\n  analysisPartial = true;\n  fallbackReason = fallbackReason || `Falha ao acessar: ${Array.from(new Set(failureSources)).join(', ')}`;\n  console.error(JSON.stringify({\n    level: 'ERROR',\n    timestamp: new Date().toISOString(),\n    step: 'PARTIAL_ANALYSIS',\n    service_name: input.service_name,\n    namespace: input.namespace,\n    error_type: 'DependencyFailure',\n    error_message: fallbackReason\n  }));\n}\n\nconst partialAnalysis = buildPartialAnalysis(kubernetesEvidence, codeEvidence, networkEvidence, triageReasoning);\n\nconst reportData = {\n  ticket_id: ticketId,\n  timestamp,\n  service_name: sanitizeText(input.service_name || 'serviço não informado'),\n  namespace: sanitizeText(input.namespace || 'namespace não informado'),\n  summary: sanitizeText(input.summary) || 'Resumo não fornecido; revisar evidências coletadas.',\n  classification: normalize(input.classification, ['infrastructure', 'code', 'unknown'], 'unknown'),\n  severity: normalize(input.severity, ['critical', 'high', 'medium', 'low'], 'medium'),\n  confidence: normalize(input.confidence, ['alta', 'média', 'baixa'], 'média'),\n  error_message: sanitizeText(input.error_message || 'Erro não informado'),\n  stack_trace: sanitizeText(input.stack_trace || ''),\n  kubernetes_evidence: sanitizeText(kubernetesEvidence),\n  code_evidence: sanitizeText(codeEvidence),\n  network_evidence: sanitizeText(networkEvidence),\n  root_cause: sanitizeText(input.root_cause) || 'Causa raiz não identificada automaticamente.',\n  suggestions: suggestions.length\n    ? suggestions\n    : ['Revisar logs completos e métricas do serviço', 'Validar configurações e limites de recursos'],\n  next_steps: nextSteps.length\n    ? nextSteps\n    : ['Confirmar correção em ambiente de homologação', 'Monitorar o serviço após o ajuste'],\n  feedback_url: sanitizeText(input.feedback_url || input.feedbackUrl || input.feedback)\n    || `https://feedback.local/diagnosis/${ticketId}`,\n  analysis_partial: analysisPartial,\n  fallback_reason: fallbackReason,\n  partial_analysis: partialAnalysis,\n};\n\nconst compiled = Handlebars.compile(template, { noEscape: true });\nconst report = compiled(reportData);\n\nreturn [\n  {\n    json: {\n      ...input,\n      ticket_id: ticketId,\n      timestamp,\n      diagnosis_report: report,\n      report_markdown: report,\n      report_data: reportData,\n      analysis_partial: analysisPartial,\n      fallback_reason: fallbackReason,\n      partial_analysis: partialAnalysis,\n    }\n  }\n];"
      },
      "id": "13",
      "name": "Gerar Relatorio",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1740, 120]
    },
    {
      "parameters": {
        "language": "javascript",
        "jsCode": "const input = $json;\n\nconst annotationSources = [\n  input?.deployment?.annotations,\n  input?.deployment?.metadata?.annotations,\n  input?.kubernetes?.deployment?.annotations,\n  input?.kubernetes?.deployment?.metadata?.annotations,\n  input?.deployment_annotations,\n];\n\nlet metadataRaw;\nfor (const annotations of annotationSources) {\n  if (annotations && annotations['project.info/metadata']) {\n    metadataRaw = annotations['project.info/metadata'];\n    break;\n  }\n}\n\nlet ownerEmail;\nif (metadataRaw) {\n  try {\n    const parsed = typeof metadataRaw === 'string' ? JSON.parse(metadataRaw) : metadataRaw;\n    ownerEmail = parsed?.owner_email || parsed?.ownerEmail;\n  } catch (error) {\n    ownerEmail = undefined;\n  }\n}\n\nownerEmail = ownerEmail || input.owner_email;\n\nif (!ownerEmail) {\n  ownerEmail = $env.DEFAULT_ALERT_EMAIL || 'devops@empresa.com';\n}\n\nreturn [\n  {\n    json: {\n      ...input,\n      owner_email: ownerEmail\n    }\n  }\n];"
      },
      "id": "14",
      "name": "Extrair Email Destinatario",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1960, 120]
    },
    {
      "parameters": {
        "language": "javascript",
        "jsCode": "const input = $json;\n\nconst classification = String(input.classification || 'unknown');\nconst classificationUpper = classification.toUpperCase();\nconst ticketId = input.ticket_id || input.ticketId || input.report_data?.ticket_id || '';\nconst ticketShort = ticketId ? String(ticketId).slice(0, 8) : '--------';\nconst serviceName = input.service_name || 'serviço não informado';\nconst namespace = input.namespace || 'namespace não informado';\nconst summary = input.summary || input.report_data?.summary || 'Resumo não disponível.';\nconst report = input.diagnosis_report || input.report_markdown || '';\nconst feedbackUrl = input.feedback_url || input.report_data?.feedback_url || '';\n\nconst analysisPartial = Boolean(\n  input.analysis_partial ||\n  input.fallback_reason ||\n  input.report_data?.analysis_partial ||\n  input.llm_fallback\n);\n\nconst fallbackReason = input.fallback_reason\n  || input.report_data?.fallback_reason\n  || (analysisPartial ? 'Falha durante processamento' : '');\n\nconst partialAnalysis = input.partial_analysis\n  || input.report_data?.partial_analysis\n  || '';\n\nconst errorMessage = input.error_message || input.report_data?.error_message || 'Erro não informado';\nconst stackTrace = input.stack_trace || input.report_data?.stack_trace || '';\nconst podName = input.pod_name || input.report_data?.pod_name || 'pod não informado';\n\nlet subject;\nlet emailBody;\n\nif (analysisPartial) {\n  subject = `[Auto-Troubleshooting] FALHA - ${serviceName} - Ticket ${ticketShort}`;\n  console.error(JSON.stringify({\n    level: 'ERROR',\n    timestamp: new Date().toISOString(),\n    step: 'EMAIL_FALLBACK',\n    service_name: serviceName,\n    namespace,\n    error_type: 'FallbackEmail',\n    error_message: fallbackReason\n  }));\n\n  const partialSection = partialAnalysis\n    ? ['---', '', '## Análise Parcial', '', partialAnalysis]\n    : [];\n\n  const stackSection = stackTrace\n    ? ['**Stack Trace:**', '```', stackTrace, '```']\n    : [];\n\n  emailBody = [\n    '# ⚠️ Alerta de Erro - Análise Automática Indisponível',\n    '',\n    `**Ticket ID:** ${ticketId}`,\n    `**Data:** ${input.timestamp || new Date().toISOString()}`,\n    `**Serviço:** ${serviceName}`,\n    `**Namespace:** ${namespace}`,\n    '',\n    '---',\n    '',\n    '## Aviso',\n    '',\n    'O sistema de Auto-Troubleshooting não conseguiu completar a análise automática deste incidente.',\n    '',\n    `**Motivo:** ${fallbackReason}`,\n    '',\n    '---',\n    '',\n    '## Alerta Original',\n    '',\n    '**Erro:**',\n    '```',\n    errorMessage,\n    '```',\n    ...stackSection,\n    '',\n    ...partialSection,\n    '',\n    '---',\n    '',\n    '## Próximos Passos',\n    '',\n    `1. Verifique manualmente o pod: kubectl describe pod ${podName} -n ${namespace}`,\n    `2. Consulte os logs: kubectl logs ${podName} -n ${namespace}`,\n    '3. Se o problema persistir, escale para o time responsável',\n    '',\n    '---',\n    '',\n    '*Este é um e-mail automático do sistema Auto-Troubleshooting.*',\n    '*A análise automática falhou, mas o alerta foi encaminhado para sua atenção.*',\n  ].join('\\n');\n} else {\n  subject = `[Auto-Troubleshooting] ${classificationUpper} - ${serviceName} - Ticket ${ticketShort}`;\n  emailBody = [\n    '# Diagnóstico Automático',\n    '',\n    `**Ticket ID:** ${ticketId}`,\n    `**Serviço:** ${serviceName}`,\n    `**Namespace:** ${namespace}`,\n    `**Classificação:** ${classification}`,\n    '',\n    '---',\n    '',\n    '## Resumo',\n    '',\n    summary,\n    '',\n    '---',\n    '',\n    '## Relatório Completo',\n    '',\n    report,\n    '',\n    '---',\n    '',\n    '📧 Para dar feedback sobre este diagnóstico, acesse:',\n    feedbackUrl,\n    '',\n    '---',\n    '',\n    '*Este é um e-mail automático do sistema Auto-Troubleshooting Inteligente.*',\n    '*Não responda a este e-mail.*',\n  ].join('\\n');\n}\n\nreturn [\n  {\n    json: {\n      ...input,\n      email_subject: subject,\n      email_body: emailBody\n    }\n  }\n];"
      },
      "id": "15",
      "name": "Montar Email",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2180, 120]
    },
    {
      "parameters": {
        "fromEmail": "={{$env.SMTP_FROM}}",
        "toEmail": "={{$json.owner_email}}",
        "subject": "={{$json.email_subject}}",
        "text": "={{$json.email_body}}",
        "options": {
          "appendAttribution": false
        }
      },
      "id": "16",
      "name": "Enviar Email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2,
      "position": [2400, 120],
      "settings": {
        "retryOnFail": true,
        "maxTries": 3,
        "waitBetweenTries": 5000
      },
      "credentials": {
        "smtp": {
          "id": "1",
          "name": "SMTP Corporativo"
        }
      }
    },
    {
      "parameters": {
        "responseBody": "ok",
        "responseData": "text",
        "responseCode": 200
      },
      "id": "5",
      "name": "Responder 200",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1080, 240]
    },
    {
      "parameters": {
        "keepOnlySet": true,
        "values": {
          "string": [
            {
              "name": "error",
              "value": "={{$json.errorMessage}}"
            }
          ]
        }
      },
      "id": "6",
      "name": "Montar Erro",
      "type": "n8n-nodes-base.set",
      "typeVersion": 2,
      "position": [860, 380]
    },
    {
      "parameters": {
        "responseBody": "={{$json.error}}",
        "responseData": "text",
        "responseCode": 400
      },
      "id": "7",
      "name": "Responder 400",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1080, 380]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Validar Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validar Payload": {
      "main": [
        [
          {
            "node": "Payload valido?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Payload valido?": {
      "main": [
        [
          {
            "node": "Log Alert",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Montar Erro",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Log Alert": {
      "main": [
        [
          {
            "node": "Responder 200",
            "type": "main",
            "index": 0
          },
          {
            "node": "LLM - Triagem",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "LLM - Triagem": {
      "main": [
        [
          {
            "node": "Classificacao?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classificacao?": {
      "main": [
        [
          {
            "node": "Rota Infrastructure",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Rota Code",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Rota Unknown",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rota Infrastructure": {
      "main": [
        [
          {
            "node": "Gerar Relatorio",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rota Code": {
      "main": [
        [
          {
            "node": "Gerar Relatorio",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rota Unknown": {
      "main": [
        [
          {
            "node": "Gerar Relatorio",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Gerar Relatorio": {
      "main": [
        [
          {
            "node": "Extrair Email Destinatario",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extrair Email Destinatario": {
      "main": [
        [
          {
            "node": "Montar Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Montar Email": {
      "main": [
        [
          {
            "node": "Enviar Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Montar Erro": {
      "main": [
        [
          {
            "node": "Responder 400",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  }
}
